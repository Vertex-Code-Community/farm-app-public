@* Headless: measures the interactive map controls (elements marked data-map-chrome) and reports their
   viewport rects to the native touch resolver via IMapControlsBoundsService. No visual output. *@
