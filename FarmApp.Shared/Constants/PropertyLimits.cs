namespace FarmApp.Shared.Constants;

public static partial class Constants
{
    public static class PropertyLimits
    {
        /// <summary>Hard upper bound on a single field's area (hectares).</summary>
        public const float MaxFieldAreaHectares = 100f;
    }
}
